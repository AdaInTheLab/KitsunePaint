# Dependencies
node_modules

# Build output
dist
dist-ssr

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Editor
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json

# Local exploration / probe scripts ~ used to investigate bundle internals
# during development, not meant to ship. Add new probes following these
# naming conventions and they'll be auto-ignored.
scripts/extract_*.py
scripts/probe_*.py
scripts/list_*.py

# Output of scripts/test_image_pipeline.py ~ regenerated each run, not meant
# to be committed. Fixtures + extracted bundle outputs.
scripts/_test_pipeline_output/
